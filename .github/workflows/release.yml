name: Release
permissions:
  contents: write
on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+*"

jobs:
  deploy:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/cache@v3
        with:
          path: |
            ~/.cache/pip
            ~/.platformio/.cache
          key: ${{ runner.os }}-pio
      - uses: actions/setup-python@v4
        with:
          python-version: "3.9"
      - name: Install PlatformIO Core
        run: pip install --upgrade platformio

      - name: Enable Long Paths
        run: git config --system core.longpaths true

      - name: Build Project
        run: |
          .\build.ps1
          ls build

      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          prerelease: ${{ endsWith(github.ref, 'nightly') || endsWith(github.ref, 'beta') }}
          files: |
            build/firmware.bin
            build/firmware.elf
            build/firmware.uf2
